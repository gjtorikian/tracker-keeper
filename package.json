
{
  "name": "tracker-keeper",
  "version": "0.0.1",
  "description": "Popover for embedded content to notify you when you're being tracked",
  "files": [
    "src/**/*",
    "dist/**/*"
  ],
  "main": "dist/index.js",
  "scripts": {
    "compile": "gulp",
    "prepublish": "npm run compile",
    "test": "mocha --timeout 10000"
  },
  "devDependencies": {
    "babel-plugin-transform-es2015-modules-umd": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-gh-pages": "^0.5.4",
    "gulp-uglify": "^1.5.3",
    "mocha": "^2.2.1",
    "selenium-webdriver": "^2.2.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.7.2"
  }
}
